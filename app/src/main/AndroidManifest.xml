<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.leemeya2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu_principal" />
        <activity android:name=".ListaMasLeidos" />
        <activity android:name=".DetalleCuentosMasLeidos" />
        <activity android:name=".DetalleCuentosMoraleja" />
        <activity android:name=".ListaCuentosMoraleja" />
        <activity android:name=".ListaCuentosClasicos" />
        <activity android:name=".DetalleCuentosClasicos" />
        <activity android:name=".ListaCuentosTradicionales" />
        <activity android:name=".DetalleCuentosTradicionales" />
        <activity android:name=".Comentarios"></activity>
    </application>

</manifest>